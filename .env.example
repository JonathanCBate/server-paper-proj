# Default values — copied to .env automatically on first ./setup.sh run
# Use 'latest' for the newest Paper build, or a specific version (1.21.11 or 26.1.2)
MC_VERSION=latest
VELOCITY_VERSION=
MEMORY=2G
CONSOLE_MODE=tmux
